name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
      - develop
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
  PYTHON_VERSION: '3.14'
  UV_VERSION: '0.11.7'

jobs:
  quality:
    name: 01 Quality checks — Python 3.14
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Cache pip / uv wheel
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
        with:
          path: |
            ~/.cache/pip
            ~/.cache/pip/wheels
          key: ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-${{ env.PYTHON_VERSION }}-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-${{ env.PYTHON_VERSION }}-
            ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-
            ${{ runner.os }}-pip-

      - name: Install uv and sync dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install uv==${{ env.UV_VERSION }}
          uv sync --frozen

      - name: Run quality checks
        run: |
          uv run ruff check .
          uv run ruff format --check .

  unit:
    name: 02 Unit tests — Python 3.14
    runs-on: ubuntu-latest
    needs: quality
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Cache pip / uv wheel
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
        with:
          path: |
            ~/.cache/pip
            ~/.cache/pip/wheels
          key: ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-${{ env.PYTHON_VERSION }}-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-${{ env.PYTHON_VERSION }}-
            ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-
            ${{ runner.os }}-pip-

      - name: Cache pytest results
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
        with:
          path: .pytest_cache
          key: ${{ runner.os }}-pytest-${{ env.PYTHON_VERSION }}-${{ hashFiles('**/pyproject.toml') }}
          restore-keys: ${{ runner.os }}-pytest-

      - name: Install uv and sync dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install uv==${{ env.UV_VERSION }}
          uv sync --frozen

      - name: Run unit tests
        run: uv run pytest -q -m "unit"

      - name: Upload coverage
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: coverage-unit-py${{ env.PYTHON_VERSION }}
          path: htmlcov

  migrations:
    name: 03 Verify Alembic migrations (PostgreSQL 17)
    runs-on: ubuntu-latest
    needs: unit
    timeout-minutes: 15
    env:
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/data_pipeline
      DATABASE_URL_RAW: postgresql://postgres:postgres@127.0.0.1:5432/data_pipeline
    services:
      postgres:
        image: pgvector/pgvector:pg17
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: data_pipeline
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d data_pipeline"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Cache pip / uv wheel
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
        with:
          path: |
            ~/.cache/pip
            ~/.cache/pip/wheels
          key: ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-${{ env.PYTHON_VERSION }}-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-${{ env.PYTHON_VERSION }}-
            ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-

      - name: Install uv and sync dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install uv==${{ env.UV_VERSION }}
          uv sync --frozen

      - name: Wait for Postgres
        run: |
          echo "Waiting for Postgres to be ready..."
          for i in $(seq 1 30); do
            pg_isready -h 127.0.0.1 -p 5432 -U postgres && break || sleep 1
          done

      - name: Test fresh migrations (upgrade from base to head)
        run: |
          set -euo pipefail
          echo "Testing fresh migrations: base -> head"
          uv run alembic upgrade head

      - name: Verify schema objects (materialized views, partitions, extensions)
        run: |
          set -euo pipefail
          uv run python - <<'PYTHON'
          import os
          import psycopg

          db_url = os.environ["DATABASE_URL_RAW"]
          conn = psycopg.connect(db_url)
          cur = conn.cursor()

          cur.execute("""
            SELECT EXISTS(
              SELECT 1 FROM pg_extension WHERE extname = 'vector'
            )
          """)
          has_vector = cur.fetchone()[0]
          print(f"✓ pgvector extension: {'present' if has_vector else 'MISSING'}")
          assert has_vector, "pgvector extension not found"

          cur.execute("""
            SELECT EXISTS(
              SELECT 1 FROM pg_matviews
              WHERE matviewname = 'records_hourly_stats'
            )
          """)
          has_view = cur.fetchone()[0]
          print(f"✓ records_hourly_stats view: {'present' if has_view else 'MISSING'}")
          assert has_view, "Materialized view records_hourly_stats not found"

          cur.execute("""
            SELECT EXISTS(
              SELECT 1 FROM information_schema.tables
              WHERE table_name = 'records_archive'
            )
          """)
          has_archive = cur.fetchone()[0]
          print(f"✓ records_archive partitioned table: {'present' if has_archive else 'MISSING'}")
          assert has_archive, "Partitioned table records_archive not found"

          cur.execute("""
            SELECT COUNT(*)
            FROM pg_inherits
            JOIN pg_class parent ON pg_inherits.inhparent = parent.oid
            JOIN pg_class child ON pg_inherits.inhrelid = child.oid
            WHERE parent.relname = 'records_archive'
          """)
          partition_count = cur.fetchone()[0]
          print(f"✓ Partitions created: {partition_count} partitions exist")
          assert partition_count > 0, "No partitions found for records_archive"

          cur.execute("""
            SELECT indexname FROM pg_indexes
            WHERE tablename = 'records'
            ORDER BY indexname
          """)
          indexes = [row[0] for row in cur.fetchall()]
          print(f"✓ Indexes on records table: {', '.join(indexes) if indexes else 'none'}")

          cur.execute("""
            SELECT constraint_name FROM information_schema.table_constraints
            WHERE table_name = 'records' AND constraint_type = 'UNIQUE'
            ORDER BY constraint_name
          """)
          unique_constraints = [row[0] for row in cur.fetchall()]
          print(f"✓ Unique constraints on records: {', '.join(unique_constraints) if unique_constraints else 'none'}")
          assert len(unique_constraints) > 0, "No unique constraints found on records table"

          cur.execute("""
            SELECT EXISTS(
              SELECT 1 FROM information_schema.tables
              WHERE table_name = 'processed_events'
            )
          """)
          has_events_table = cur.fetchone()[0]
          print(f"✓ processed_events table: {'present' if has_events_table else 'MISSING'}")
          assert has_events_table, "processed_events table not found"

          cur.close()
          conn.close()
          print("\n✅ All schema objects verified successfully")
          PYTHON

      - name: Test migration idempotency (downgrade + upgrade)
        run: |
          set -euo pipefail
          echo "Testing migration idempotency: head -> base -> head"

          uv run alembic downgrade base

          uv run python - <<'PYTHON'
          import os
          import psycopg

          db_url = os.environ["DATABASE_URL_RAW"]
          conn = psycopg.connect(db_url)
          cur = conn.cursor()

          cur.execute("""
            SELECT COUNT(*) FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name IN ('records', 'processed_events')
          """)
          remaining_tables = cur.fetchone()[0]
          assert remaining_tables == 0, f"Expected 0 tables after downgrade, found {remaining_tables}"
          print("✓ All tables removed on downgrade")

          cur.close()
          conn.close()
          PYTHON

          uv run alembic upgrade head

          uv run python - <<'PYTHON'
          import os
          import psycopg

          db_url = os.environ["DATABASE_URL_RAW"]
          conn = psycopg.connect(db_url)
          cur = conn.cursor()

          cur.execute("""
            SELECT COUNT(*) FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name IN ('records', 'processed_events')
          """)
          table_count = cur.fetchone()[0]
          assert table_count == 2, f"Expected 2 tables after re-upgrade, found {table_count}"
          print("✓ All tables restored on re-upgrade")

          cur.close()
          conn.close()
          PYTHON

      - name: Test insert into all tables (constraint check)
        run: |
          set -euo pipefail
          uv run python - <<'PYTHON'
          import os
          from datetime import datetime, timezone

          import psycopg

          db_url = os.environ["DATABASE_URL_RAW"]
          conn = psycopg.connect(db_url)
          cur = conn.cursor()

          now = datetime.now(timezone.utc).replace(tzinfo=None)
          cur.execute("""
            INSERT INTO records (source, timestamp, raw_data, tags, processed, created_at)
            VALUES (%s, %s, %s, %s, %s, %s)
            RETURNING id
          """, ("test-source", now, '{"value": 42}', '[]', False, now))
          record_id = cur.fetchone()[0]
          print(f"✓ Successfully inserted record (id={record_id})")

          cur.execute("""
            INSERT INTO processed_events (
              kafka_topic, kafka_partition, kafka_offset, idempotency_key,
              event_type, payload, status, processing_attempts,
              dead_letter_queue, created_at
            )
            VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
            RETURNING id
          """, ("test-topic", 0, 0, "test-key-1", "test.event", '{}', "pending", 0, False, now))
          event_id = cur.fetchone()[0]
          print(f"✓ Successfully inserted processed_event (id={event_id})")

          try:
            cur.execute("""
              INSERT INTO records (source, timestamp, raw_data, tags, processed, created_at)
              VALUES (%s, %s, %s, %s, %s, %s)
            """, ("test-source", now, '{"value": 99}', '[]', False, now))
            print("✗ FAILED: Unique constraint not enforced on (source, timestamp)")
            raise AssertionError("Unique constraint not enforced")
          except psycopg.IntegrityError:
            print("✓ Unique constraint enforced on records (source, timestamp)")

          try:
            cur.execute("""
              INSERT INTO processed_events (
                kafka_topic, kafka_partition, kafka_offset, idempotency_key,
                event_type, payload, status, processing_attempts,
                dead_letter_queue, created_at
              )
              VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, ("topic2", 0, 0, "test-key-1", "test.event", '{}', "pending", 0, False, now))
            print("✗ FAILED: idempotency_key unique constraint not enforced")
            raise AssertionError("idempotency_key constraint not enforced")
          except psycopg.IntegrityError:
            print("✓ Unique constraint enforced on processed_events (idempotency_key)")

          conn.commit()
          cur.close()
          conn.close()
          print("\n✅ All constraint tests passed")
          PYTHON

  integration:
    name: 04 Integration tests — Python 3.14
    runs-on: ubuntu-latest
    needs: migrations
    timeout-minutes: 60
    services:
      postgres:
        image: pgvector/pgvector:pg17
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_database
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d test_database"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis:7
        options: --health-cmd "redis-cli ping" --health-interval 10s --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Cache pip / uv wheel
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
        with:
          path: |
            ~/.cache/pip
            ~/.cache/pip/wheels
          key: ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-${{ env.PYTHON_VERSION }}-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-${{ env.PYTHON_VERSION }}-${{ hashFiles('**/pyproject.toml') }}
            ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-${{ env.PYTHON_VERSION }}-
            ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-
            ${{ runner.os }}-pip-${{ env.PYTHON_VERSION }}-
            ${{ runner.os }}-pip-

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Install uv and sync dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install uv==${{ env.UV_VERSION }}
          uv sync --frozen

      - name: Wait for Postgres
        run: |
          echo "Waiting for Postgres to be ready..."
          for i in $(seq 1 30); do
            pg_isready -h 127.0.0.1 -p 5432 -U postgres && break || sleep 1
          done

      - name: Run integration tests
        env:
          DATABASE_URL_TEST: postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/test_database
        run: |
          set -euo pipefail
          uv run pytest -q -m "integration"

      - name: Upload coverage
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: coverage-integration-py${{ env.PYTHON_VERSION }}
          path: htmlcov

  e2e:
    name: 05 E2E tests — Python 3.14
    runs-on: ubuntu-latest
    needs: integration
    timeout-minutes: 90
    services:
      postgres:
        image: pgvector/pgvector:pg17
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_database
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d test_database"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis:7
        options: --health-cmd "redis-cli ping" --health-interval 10s --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Cache pip / uv wheel
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
        with:
          path: |
            ~/.cache/pip
            ~/.cache/pip/wheels
          key: ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-${{ env.PYTHON_VERSION }}-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-${{ env.PYTHON_VERSION }}-${{ hashFiles('**/pyproject.toml') }}
            ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-${{ env.PYTHON_VERSION }}-
            ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-
            ${{ runner.os }}-pip-${{ env.PYTHON_VERSION }}-
            ${{ runner.os }}-pip-

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Install uv and sync dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install uv==${{ env.UV_VERSION }}
          uv sync --frozen

      - name: Wait for Postgres
        run: |
          echo "Waiting for Postgres to be ready..."
          for i in $(seq 1 30); do
            pg_isready -h 127.0.0.1 -p 5432 -U postgres && break || sleep 1
          done

      - name: Run E2E tests
        env:
          DATABASE_URL_TEST: postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/test_database
        run: |
          set -euo pipefail
          uv run pytest -q -m "e2e"

  dependency-audit:
    name: 06 Dependency Audit
    runs-on: ubuntu-latest
    needs: e2e
    if: ${{ github.event_name == 'pull_request' }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Cache pip / uv wheel
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
        with:
          path: |
            ~/.cache/pip
            ~/.cache/pip/wheels
          key: ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-${{ env.PYTHON_VERSION }}-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-${{ env.PYTHON_VERSION }}-
            ${{ runner.os }}-pip-uv-${{ env.UV_VERSION }}-
            ${{ runner.os }}-pip-

      - name: Install uv and sync dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install uv==${{ env.UV_VERSION }} pip-audit
          uv sync --frozen

      - name: Run pip-audit
        run: pip-audit --progress-spinner off --desc

  build-images:
    name: 07 Docker build — ${{ matrix.image.name }}
    strategy:
      matrix:
        image:
          - name: ingestor
            image_name: data-zoo/ingestor
            dockerfile: Dockerfile
          - name: ai_gateway
            image_name: data-zoo/ai_gateway
            dockerfile: services/ai_gateway/Dockerfile
          - name: query_api
            image_name: data-zoo/query_api
            dockerfile: services/query_api/Dockerfile
          - name: processor
            image_name: data-zoo/processor
            dockerfile: services/processor/Dockerfile
          - name: dashboard
            image_name: data-zoo/dashboard
            dockerfile: services/dashboard/Dockerfile
    needs:
      - quality
      - unit
      - migrations
      - integration
      - e2e
    if: ${{ github.event_name != 'pull_request' }}
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/docker-build-reusable.yml
    with:
      image-name: ${{ matrix.image.image_name }}
      dockerfile: ${{ matrix.image.dockerfile }}
      context: .
      aws-region: ${{ vars.AWS_REGION }}
      aws-role-arn: ${{ vars.AWS_ROLE_ARN }}
      push-image: false
    secrets:
      aws_account_id: ${{ secrets.AWS_ACCOUNT_ID }}

  build-summary:
    name: 07 Docker build summary
    runs-on: ubuntu-latest
    needs: build-images
    if: ${{ github.event_name != 'pull_request' }}
    steps:
      - name: Report build matrix
        run: |
          echo "### Docker Build Result" >> "$GITHUB_STEP_SUMMARY"
          echo "Built images:" >> "$GITHUB_STEP_SUMMARY"
          echo "- data-zoo/ingestor" >> "$GITHUB_STEP_SUMMARY"
          echo "- data-zoo/ai_gateway" >> "$GITHUB_STEP_SUMMARY"
          echo "- data-zoo/query_api" >> "$GITHUB_STEP_SUMMARY"
          echo "- data-zoo/processor" >> "$GITHUB_STEP_SUMMARY"
          echo "- data-zoo/dashboard" >> "$GITHUB_STEP_SUMMARY"
          echo "- Push mode: disabled (CI build-only)" >> "$GITHUB_STEP_SUMMARY"
