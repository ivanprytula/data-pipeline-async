name: CI — Integration & E2E

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: ci-integration-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read  # Read repo for checkout; no other permissions needed

jobs:
  tests:
    name: Integration/E2E — ${{ matrix.test-type }} (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.14"]
        test-type: ["integration", "e2e"]

    services:
      postgres:
        image: pgvector/pgvector:pg17
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_database
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d test_database"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis:7
        options: --health-cmd "redis-cli ping" --health-interval 10s --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v4.3.1

      - name: Cache pip (keyed by uv.lock)
        uses: actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v4.0.0
        with:
          path: |
            ~/.cache/pip
            ~/.cache/pip/wheels
          key: ${{ runner.os }}-pip-uv-0.11.7-${{ matrix.python-version }}-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-pip-uv-0.11.7-${{ matrix.python-version }}-${{ hashFiles('**/pyproject.toml') }}
            ${{ runner.os }}-pip-uv-0.11.7-${{ matrix.python-version }}-
            ${{ runner.os }}-pip-uv-0.11.7-
            ${{ runner.os }}-pip-${{ matrix.python-version }}-
            ${{ runner.os }}-pip-

      - name: Set up Python
        uses: actions/setup-python@3235b876344d2a9aa001b8d1453c930bba69e610 # v3.9.1
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install `uv` and sync dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install uv==0.11.7
          uv sync --frozen

      - name: Wait for Postgres
        run: |
          echo "Waiting for Postgres to be ready..."
          for i in $(seq 1 30); do
            pg_isready -h 127.0.0.1 -p 5432 -U postgres && break || sleep 1
          done

      - name: Run tests
        env:
          TEST_TYPE: ${{ matrix.test-type }}
          DATABASE_URL_TEST: postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/test_database
        run: |
          set -euo pipefail
          case "$TEST_TYPE" in
            integration)
              uv run pytest -q -m "integration"
              ;;
            e2e)
              uv run pytest -q -m "e2e"
              ;;
            *)
              uv run pytest -q
              ;;
          esac

      - name: Upload coverage (integration)
        if: ${{ matrix.test-type == 'integration' }}
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v4.5.0
        with:
          name: coverage-integration-py${{ matrix.python-version }}
          path: htmlcov
