apiVersion: v1
kind: Service
metadata:
  name: ai-gateway
  labels:
    app.kubernetes.io/name: ai-gateway
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-gateway
  ports:
    - name: http
      port: 8001
      targetPort: http
      protocol: TCP
