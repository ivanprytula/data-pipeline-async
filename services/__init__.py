"""Services package — microservices for Data Zoo Platform.

- `processor/` — Kafka consumer, event processing
- `inference/` — Embeddings, vector search (Qdrant integration)
- `analytics/` — Read-optimized analytics (Phase 5)
- `dashboard/` — HTMX frontend (Phase 6)
"""
